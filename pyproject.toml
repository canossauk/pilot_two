[project]
name = "pilot-two"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.128.0",
    "flet[all]>=0.80.4",
    "httpx>=0.28.1",
    "sqlmodel>=0.0.31",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
